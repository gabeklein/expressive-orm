{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"]
    },
    "version": {
      "dependsOn": ["build"],
      "outputs": ["CHANGELOG.md", "package.json"]
    },
    "publish": {
      "dependsOn": ["build", "test", "version"],
      "outputs": []
    }
  }
}